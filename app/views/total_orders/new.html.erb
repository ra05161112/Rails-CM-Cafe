<%= form_with(model: @total_order, url: "/total_orders", local: true) do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.fields_for :orders do |of| %>
    <%= render "order_field", f: of %>
  <% end %>
  
  <%= f.submit "登録" %>
<% end %>
